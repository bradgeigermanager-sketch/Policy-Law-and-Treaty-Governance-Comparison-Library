# UI

UI schemas describe logical panels and dashboards:

- `ui.governance_impact_dashboard.*`
- `ui.law_participant_impact_panel.*`

They are view models, not rendering instructions. Any frontend can bind to them.
